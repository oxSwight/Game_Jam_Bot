.env
players.db
*.db
# SQLite WAL/shared-memory sidecars carry live row data (PII) - never commit.
*.db-shm
*.db-wal
__pycache__/
*.pyc
.venv/
venv/
.bot.lock
.pytest_cache/
.ruff_cache/

# Local growth/marketing drafts - deliberately never committed.
marketing/

# Local database dumps (PII) - take one before any DB surgery, never commit it.
backups/

*.log
